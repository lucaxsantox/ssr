import styles from './Button.module.css'

export default function ButtonBack() {
    return (
        <button type="button"
        className={styles.error}>
            Previous
        </button>
    )
}